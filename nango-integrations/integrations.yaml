integrations:
  - slack:
      base_url: https://slack.com/api/
      call_auth:
        mode: AUTH_HEADER_TOKEN
      log_level: debug
  - github:
      base_url: https://api.github.com/
      call_auth:
        mode: AUTH_HEADER_TOKEN
      log_level: debug
