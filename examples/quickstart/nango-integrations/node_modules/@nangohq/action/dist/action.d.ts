import type { HttpHeader, HttpParams, NangoConfig, NangoConnection, NangoIntegrationConfig } from '@nangohq/core';
import type * as winston from 'winston';
import type { AxiosResponse, Method } from 'axios';
declare class NangoAction {
    private nangoConfig;
    private integrationConfig;
    private userConnection;
    private axiosInstance;
    private actionName;
    private executionStartTime;
    protected logger: winston.Logger;
    constructor(nangoConfig: NangoConfig, integrationConfig: NangoIntegrationConfig, userConnection: NangoConnection, logger: winston.Logger, actionName: string);
    markExecutionComplete(): void;
    protected getCurrentConnectionConfig(): NangoConnection;
    protected httpRequest(endpoint: string, method: Method, params?: HttpParams, body?: any, headers?: HttpHeader): Promise<AxiosResponse>;
    executeAction(input: any): Promise<any>;
}
export { NangoAction };
