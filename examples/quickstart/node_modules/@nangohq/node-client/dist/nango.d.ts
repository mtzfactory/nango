import { NangoMessageHandlerResult } from '@nangohq/core';
export default class Nango {
    /** -------------------- Private Properties -------------------- */
    private sendQueueId;
    private receiveQueueId;
    private connection?;
    private sendChannel?;
    private receiveChannel?;
    private nangoServerHost?;
    private nangoServerPort?;
    /** -------------------- Public Methods -------------------- */
    constructor(host?: string, port?: number);
    connect(): Promise<void>;
    registerConnection(integration: string, userId: string, oAuthAccessToken: string, additionalConfig?: Record<string, unknown>): Promise<NangoMessageHandlerResult>;
    triggerAction(integration: string, triggerAction: string, userId: string, input: Record<string, unknown>): Promise<NangoMessageHandlerResult>;
    close(): void;
    /** -------------------- Private Methods -------------------- */
    private sendMessageToServer;
    private connectRabbit;
}
